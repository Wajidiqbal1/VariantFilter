% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_vcf_tidy.R
\name{filter_vcf_tidy}
\alias{filter_vcf_tidy}
\title{Filter VCF Data}
\usage{
filter_vcf_tidy(
  input_file,
  output_file,
  zygosity_column,
  zygosity_value,
  maf_criteria,
  exome_column,
  genome_column,
  region_column,
  region_filters,
  effect_column,
  effect_filters,
  gerp_column = NULL,
  gerp_criteria = NULL,
  cadd_column = NULL,
  cadd_criteria = NULL,
  custom_filters = NULL
)
}
\arguments{
\item{input_file}{The path to the input file (.csv or .xlsx).}

\item{output_file}{The path to the output file (.csv).}

\item{zygosity_column}{The column name for zygosity filtering.}

\item{zygosity_value}{The value for zygosity filtering (e.g., "het" or "hom").}

\item{maf_criteria}{The criteria for MAF filtering (e.g., "<=0.001").}

\item{exome_column}{The column name for exome data. Missing values are replaced with 10.}

\item{genome_column}{The column name for genome data. Missing values are replaced with 10.}

\item{region_column}{The column name for region filtering.}

\item{region_filters}{A vector of filters for region filtering (e.g., c("exonic")).}

\item{effect_column}{The column name for effect filtering.}

\item{effect_filters}{A vector of filters for effect filtering (e.g., c("nonsynonymous SNV")).}

\item{gerp_column}{The column name for GERP filtering (optional).}

\item{gerp_criteria}{The criteria for GERP filtering (optional, e.g., ">=3").}

\item{cadd_column}{The column name for CADD filtering (optional).}

\item{cadd_criteria}{The criteria for CADD filtering (optional, e.g., ">=20").}

\item{custom_filters}{A list of custom filters (optional). Each filter is a list
with two elements: \code{column} (the column name) and \code{criteria} (the filter criteria).}
}
\value{
A filtered data frame.
}
\description{
This function filters VCF data based on various criteria.
}
\examples{
\dontrun{
filter_vcf_tidy(
  input_file = "your_input_file.csv",
  output_file = "your_output_file.csv",
  zygosity_column = "Zygosity",
  zygosity_value = "het",
  maf_criteria = "<=0.001",
  exome_column = "gnomAD_exome_ALL",
  genome_column = "gnomAD_genome_ALL",
  region_column = "Func.refGene",
  region_filters = c("exonic"),
  effect_column = "ExonicFunc.refGene",
  effect_filters = c("nonsynonymous SNV", "stoploss"),
  gerp_column = "GERP.._RS",
  gerp_criteria = ">=3",
  custom_filters = list(
    list(column = "SIFT_pred", criteria = "== 'D'"),
    list(column = "PROVEAN_pred", criteria = "== 'D'"),
    list(column = "LRT_pred", criteria = "== 'D'")
  )
)
}
}
